import 'package:flutter/material.dart';
import 'app.dart';

void main() {
  runApp(
   // ChangeNotifierProvider(
    //  create: (_) => SessionResultsStore(),
     // child: const App(),
   // ),
   const App());
}

